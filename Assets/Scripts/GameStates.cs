using UnityEngine;

public enum GameStates
{
   PAUSED = 0, // Menu
   SAILMODE = 1, // Sailing
   MOTORMODE = 2, // Driving the boat with motor
   HUMANMODE = 3, // Walking around
   DIALOGMODE = 4 // Dialog
}
